#!/bin/sh

case "$1" in
	configure)
		newVersion=$(dpkg-query --showformat='${Version}' --show lliurex-easysites)
		tokenDir=/var/lib/lliurex-easysites/version
		echo "$newVersion" > "$tokenDir" || true
		;;
	*)
		echo "Unknow action"
		;;
esac

#DEBHELPER#
