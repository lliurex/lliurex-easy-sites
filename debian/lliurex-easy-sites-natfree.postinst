#!/bin/sh

case "$1" in
	configure)
		newVersion=$(dpkg-query --showformat='${Version}' --show lliurex-easy-sites-natfree)
      	tokenDir=/var/lib/lliurex-easy-sites-natfree/version
      	echo "$newVersion" > "$tokenDir" || true
		;;
	*)
		echo "Unknow action"
		;;
esac

#DEBHELPER#
