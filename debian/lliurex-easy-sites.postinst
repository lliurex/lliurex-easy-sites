#!/bin/sh

case "$1" in
	configure)
		NETFOLDERS_CONF="/etc/n4d/conf.d/NetFoldersManager"
		n4d-modules enable-plugin ${NETFOLDERS_CONF} || true
		;;
	*)
		echo "Unknow action"
		;;
esac

#DEBHELPER#
