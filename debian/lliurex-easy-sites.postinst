#!/bin/sh

case "$1" in
	configure)
		newVersion=$(dpkg-query --showformat='${Version}' --show lliurex-easy-sites)
      	tokenDir=/var/lib/lliurex-easy-sites/version
      	echo "$newVersion" > "$tokenDir" || true
		;;
	*)
		echo "Unknow action"
		;;
esac

#DEBHELPER#
